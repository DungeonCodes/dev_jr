# EX005 — SELECT com WHERE

Em `query.sql`, escreva uma consulta que retorne `id` e `name` da tabela `users` somente para registros ativos. Use o esquema em `fixtures/schema.sql`.

Execute: `npm run test:exercise -- ex005`
