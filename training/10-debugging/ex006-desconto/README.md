# EX006 — Debugging de desconto

`calculateDiscountedPrice` deveria aplicar `discountPercent` sobre `price`. O código contém um erro. Corrija-o sem mudar a assinatura da função.

Execute: `npm run test:exercise -- ex006`
