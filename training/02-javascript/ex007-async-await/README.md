# EX007 — Buscar perfil assíncrono

Implemente `getProfileName`. A função recebe `fetchProfile`, uma função assíncrona que retorna um perfil com `name`, e deve retornar esse nome. Propague erros do carregamento.

Execute: `npm run test:exercise -- ex007`
