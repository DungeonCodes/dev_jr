# EX003 — Propriedade opcional

Implemente `getDisplayName`. Retorne `profile.displayName` quando disponível; caso contrário, retorne `"Usuário sem nome"`. A função deve aceitar objeto sem `profile` e perfil sem nome.

Execute: `npm run test:exercise -- ex003`
