# EX004 — Categoria de status HTTP

Implemente `getHttpStatusCategory`. Classifique códigos HTTP entre `success` (2xx), `client_error` (4xx), `server_error` (5xx) e `other` para qualquer outro valor.

Execute: `npm run test:exercise -- ex004`
