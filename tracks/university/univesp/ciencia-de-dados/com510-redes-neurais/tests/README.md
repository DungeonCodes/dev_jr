# Testes da COM510

Quando existir uma tentativa Python, adicione testes `pytest` que importem a lógica de `exercises/` ou `labs/`. Não deixe lógica somente em notebook. Nesta fase ainda não há código executável nem testes a inventar.
