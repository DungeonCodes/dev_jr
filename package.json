{
  "name": "dev-jr-training",
  "private": true,
  "type": "module",
  "version": "0.1.0",
  "description": "Ambiente progressivo de treinamento para testes técnicos de Desenvolvedor Júnior.",
  "scripts": {
    "test": "node scripts/run-tests.mjs --references",
    "test:exercise": "node scripts/run-tests.mjs --exercise",
    "test:category": "node scripts/run-tests.mjs --category",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "typescript": "^5.0.0",
    "vitest": "^4.0.0"
  }
}
